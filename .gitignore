ai-coaching/img/ryan headshot zoom 2.png

# Un-redacted A-08 capture. It shows C:\Users\ryanp\… and C:\studio\… in the
# SetMaster 2 collection-path fields, which the capture brief
# (sm3-specific-pages/planning/03-assets-and-capture-list.md §1) forbids
# shipping. The redacted derivative sm3-assets/img/a08-sm2-launchpad.png is what
# the page uses and what gets committed. This original was never committed, so
# the leak is not in history — keep it that way. Safe to delete locally.
sm3-specific-pages/sm3-assets/img/sm2 launchpad screenshot.png
