# THROWAWAY — spike caller stub for runbook step 6.1 (Teams task 1). Deleted with its branch.
name: Spike caller

on:
  pull_request:
    types: [opened, reopened, synchronize, labeled, unlabeled]

permissions:
  checks: write
  contents: read
  pull-requests: write

jobs:
  gate:
    uses: wolfpackdata/wp-github-sop/.github/workflows/spike-echo.yml@spike-v0
    with:
      marker: SPIKE-6-1-PINNED-TAG
